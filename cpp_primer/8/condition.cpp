#include <iostream>

using namespace::std;

int main()
{
    int word;

    //cin.tie(0);
    cin >> word;
    cout << word;
    cin >> word;
    cout << word;
    cin >> word;
    cout << word;
    sleep(10);

    return 0;
}
